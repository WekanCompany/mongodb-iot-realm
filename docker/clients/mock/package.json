{
  "name": "mqtt-test-client",
  "version": "1.0.0",
  "description": "Pushes sample sensor data to the mqtt broker",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^4.2.3"
  }
}
